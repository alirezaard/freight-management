spring:
  application:
    name: Freight Management System
  data:
    mongodb:
      uri: mongodb://alireza:2ba2mishe22@localhost:27017/freight
  datasource:
    url: jdbc:mysql://localhost:3306/freight?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: alireza
    password: 2ba2mishe22
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update   # می‌تونه create, create-drop, update, validate باشه
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8585